spring:
  application:
    name: docker-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/liumapp/simple-docker-demo/
          search-paths: config

server:
  port: 7001

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1234/eureka/

