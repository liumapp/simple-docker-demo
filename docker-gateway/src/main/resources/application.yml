spring:
  application:
    name: @project.artifactId@

server:
  port: 2333

eureka:
  client:
    service-url:
      defaultZone: http://192.168.1.143:1234/eureka/
zuul:
  routes:
    docker-weba:
      serviceId: docker-weba
      path: /**
    docker-webb:
      serviceId: docker-webb
      path: /**


