server:
  port: 1234
eureka:
  instance:
    hostname: localhost
    prefer-ip-address:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:1234/eureka/
